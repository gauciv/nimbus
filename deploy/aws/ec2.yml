AWSTemplateFormatVersion: '2010-09-09'
Description: 'Nimbus Bot - EC2 Free Tier with Auto-Deploy'

Resources:
  BotSecurityGroup:
    Type: AWS::EC2::SecurityGroup
    Properties:
      GroupDescription: Nimbus bot security group
      SecurityGroupIngress:
        - IpProtocol: tcp
          FromPort: 22
          ToPort: 22
          CidrIp: 0.0.0.0/0

  BotInstance:
    Type: AWS::EC2::Instance
    Properties:
      ImageId: ami-0d07675d294f17973
      InstanceType: t3.micro
      KeyName: nimbus-key
      SecurityGroupIds:
        - !Ref BotSecurityGroup
      Tags:
        - Key: Name
          Value: Nimbus-Bot
      UserData:
        Fn::Base64: !Sub |
          #!/bin/bash
          yum update -y
          yum install -y docker git
          systemctl start docker
          systemctl enable docker
          usermod -a -G docker ec2-user
          
          curl -L "https://github.com/docker/compose/releases/latest/download/docker-compose-$(uname -s)-$(uname -m)" -o /usr/local/bin/docker-compose
          chmod +x /usr/local/bin/docker-compose
          
          # Create deployment directory and clone repository
          mkdir -p /home/ec2-user/nimbus
          cd /home/ec2-user
          git clone https://github.com/gauciv/nimbus.git nimbus
          chown -R ec2-user:ec2-user /home/ec2-user/nimbus

Outputs:
  InstanceIP:
    Value: !GetAtt BotInstance.PublicIp